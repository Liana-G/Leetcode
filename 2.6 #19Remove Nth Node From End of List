#19 Remove Nth Node From End of List
# 题目：Given the head of a linked list, remove the nth node from the end of the list and return its head.

# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def removeNthFromEnd(self, head: Optional[ListNode], n: int) -> Optional[ListNode]:
# 使用dummy
# 题目理解：fast 比 slow 多走 n+1 步
# 当 fast 走完链表，slow 还剩 n 步到结尾。也就是 slow 正好在倒数第 n 个节点前面

# 注意双指针的间距！
        dummy = ListNode(0)
        dummy.next = head

        fast = dummy
        slow = dummy

        for i in range(n+1):
            fast = fast.next

        while fast:
            fast = fast.next
            slow = slow.next

# 全部循环结束后，也就是fast = None之后，开始删除节点。
        slow.next = slow.next.next

# 链表的本质：只要你拿到第一个节点（head），就等于拿到了整个链表。
# 链表是“从头节点开始的一条引用链”。
        return dummy.next
