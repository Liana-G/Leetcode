# 区间和求解

# 前缀和的使用
def build_prefix_sum(array):
    prefix = [0] * len(array)
    prefix[0] = array[0]

    for i in range(1 , len(array)):
        prefix[i] = prefix[i-1] + array[i]

    return prefix[i]
