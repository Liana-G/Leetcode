# 螺旋矩阵
# 题目：给定一个正整数 n，生成一个包含 1 到 n^2 所有元素，且元素按顺时针顺序螺旋排列的正方形矩阵。

class Solution:
    def generateMatrix(self, n: int) -> List[List[int]]:
        # 生成n*n的矩阵（元素为0）
        nums = [[0] * n for _ in range(n)]
        # 定义起始点
        startx, starty = 0 , 0
        # 定义循环次数为 n//2，中间数为n//2（只有当 n 为奇数时有中间数）
        loop, mid = n // 2, n // 2
        count = 1

        for offset in range(1, loop + 1):
            for i in range(starty, n - offset): # 找到列
                nums[startx][i] = count # 注意这里是坐标，startx表示行[0]，i 表示列（从 starty 到 n-offset
                count += 1
            for i in range(startx , n-offset):
                nums[i][n - offset] = count
                count += 1
            for i in range(n - offset, starty , -1):
                nums[n - offset][i] = count
                count += 1
            for i in range(n - offset, startx, -1):
                nums[i][starty] = count
                count += 1
            startx += 1
            starty += 1

        if n % 2 != 0:
            nums[mid][mid] = count 
# 这一行的目的是赋值中心位置坐标。这里注意！！！当 n 为奇数时，下标从 0 开始的话，中心坐标为（n//2,n//2）
        return nums






        
